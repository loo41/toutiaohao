``` PC M
  PC端和M端是由vue-cli搭建而成
  根目录 cd /
  npm install
  npm run dev 进行开发
  npm run build 进行编译打包

  依赖环境 在 node > 6.0.0 下进行即可
  命令都在根目录下package下的scripts里
```

``` 小程序
  更改appid
  就打开开发者工具，导入目录即可
```


